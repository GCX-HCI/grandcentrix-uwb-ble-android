package net.grandcentrix.api.logging

/**
 * Log levels used in the library. Log messages are filtered by [LogConfig.level].
 */
enum class LogLevel {
    Verbose,
    Debug,
    Info,
    Warning,
    Error
}
