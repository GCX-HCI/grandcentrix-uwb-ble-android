package net.grandcentrix.scaffold.app.ui.theme

import androidx.compose.material.Typography

val AppTypography = Typography(
    // Place all the custom text attributes for each appearance in the specific [TextStyle] like the
    // following.

    // h1 = TextStyle(fontSize = 26.dp, ...)
)
